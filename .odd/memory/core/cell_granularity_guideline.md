---
id: "cell_granularity_guideline"
title: "Memory Cell 입자도 가이드라인"
relatedness: 1.0
references: ["relatedness_guideline", "reference"]
tags: ["core", "protocol", "granularity", "neural-network"]
---

# 🔬 Memory Cell 입자도 가이드라인 (Neural Network Edition)

## 1. 개요 (Overview)

Memory Cell Architecture는 **뇌의 신경망 구조**를 모방합니다. 각 Cell은 살아있는 뉴런이며, 연관성(Relatedness)은 시냅스입니다.

**핵심 철학**:
- 각 Cell은 **하나의 관심사(Concern)**만 담당
- Cell들은 **다차원 연관성 네트워크**로 연결
- 공통 패턴 발견 시 **새로운 Cell로 추출** (신경망의 학습처럼)
- 크기는 부차적, **관심사의 분리**가 최우선

---

## 2. Cell 분리의 핵심 원칙

### 🎯 관심사 기반 분리 (Concern-Driven Separation)

**질문**: "이 문서가 다루는 관심사가 몇 개인가?"

#### ❌ Bad: 하나의 거대한 Cell
```markdown
# user_authentication.md (600줄)

## JWT 토큰 발급
[150줄의 JWT 로직]

## 세션 관리
[120줄의 세션 로직]

## 비밀번호 암호화
[100줄의 암호화 로직]

## OAuth 연동
[200줄의 OAuth 로직]

## 2FA 처리
[180줄의 2FA 로직]
```
→ **6개의 서로 다른 관심사**가 하나의 문서에!

#### ✅ Good: 관심사별 Cell 분리
```
auth/
├─ user_authentication.md (살아있는 개요 문서)
│  └─ references: [jwt_token, session_lifecycle, password_hashing, oauth, 2fa]
├─ jwt_token_issuance.md (JWT 발급만)
├─ session_lifecycle.md (세션 생명주기만)
├─ password_hashing.md (비밀번호 암호화만)
├─ oauth_integration.md (OAuth만)
└─ two_factor_auth.md (2FA만)
```

---

## 3. 살아있는 문서 vs 세부 Cell

### 📚 살아있는 개요 문서 (Living Overview)
**역할**: 전체 맥락 제공 + 세부 Cell 연결

```markdown
# user_authentication.md (200줄)

## 개요
사용자 인증 시스템의 전체 흐름과 구성 요소

## 인증 방식
- JWT 기반 인증 → [jwt_token_issuance.md](0.95)
- OAuth 소셜 로그인 → [oauth_integration.md](0.90)
- 2단계 인증 → [two_factor_auth.md](0.85)

## 보안 메커니즘
- 비밀번호 암호화 → [password_hashing.md](0.95)
- 세션 관리 → [session_lifecycle.md](0.90)

## 감사 추적
- 로그인 이력 → [login_audit_trail.md](0.80)

## References
- `jwt_token_issuance.md` (0.95)
- `session_lifecycle.md` (0.90)
- `password_hashing.md` (0.95)
- `oauth_integration.md` (0.90)
- `two_factor_auth.md` (0.85)
- `login_audit_trail.md` (0.80)
```

### 🧬 세부 Cell (Atomic Concern)
**역할**: 하나의 관심사만 깊이 있게 설명

```markdown
# jwt_token_issuance.md (150줄)

## 개요
JWT 토큰 발급 로직

## 발급 프로세스
[상세 로직]

## 토큰 구조
[페이로드, 클레임 등]

## References
- `token_validation.md` (0.95) - 발급된 토큰 검증
- `token_refresh.md` (0.90) - 토큰 갱신
- `user_authentication.md` (0.95) - 상위 개요
```

---

## 4. 교차 관심사의 발견 및 추출

### 🔍 공통 패턴 발견

**시나리오**: 여러 Cell에서 "인증 검증 로직"이 중복 발견

```
초기 상태:
├─ user_authentication.md
│  └─ "사용자 인증 검증" 로직 포함
├─ oauth_integration.md
│  └─ "OAuth 인증 검증" 로직 포함 (중복!)
└─ two_factor_auth.md
   └─ "2FA 인증 검증" 로직 포함 (중복!)
```

### ⚡ 리팩토링: 공통 Cell 추출

```
리팩토링 후:
├─ authentication_validation.md (신규 생성!)
│  └─ 모든 인증 검증의 공통 로직
├─ user_authentication.md
│  └─ references: [authentication_validation] (0.95)
├─ oauth_integration.md
│  └─ references: [authentication_validation] (0.95)
└─ two_factor_auth.md
   └─ references: [authentication_validation] (0.95)
```

**핵심**: 뇌가 학습하면서 새로운 뉴런을 만들듯, 공통 패턴을 발견하면 새로운 Cell을 생성!

---

## 5. 다차원 연관성 네트워크

### 🕸️ 하나의 Cell, 여러 관점

`password_hashing.md`는 여러 관점에서 동시에 참조됨:

```
[로그인 관점]
login_flow.md
├─ login_audit_trail.md (0.90)
├─ password_hashing.md (0.85) ← 여기!
└─ session_lifecycle.md (0.80)

[보안 관점]
security_overview.md
├─ password_hashing.md (0.95) ← 여기!
├─ authentication_validation.md (0.90)
└─ encryption_standards.md (0.85)

[사용자 관리 관점]
user_management.md
├─ user_registration.md (0.90)
├─ password_hashing.md (0.85) ← 여기!
└─ password_reset.md (0.80)
```

**핵심**: Cell은 **하나의 카테고리에 속하지 않음**. 다차원 네트워크의 노드!

---

## 6. Cell 분리 결정 트리

```
문서를 보고 질문:

1. "이 문서가 다루는 관심사가 몇 개인가?"
   ├─ 1개 → ✅ 적절한 Cell
   └─ 2개 이상 → 분리 검토

2. "각 섹션이 독립적으로 참조될 가능성이 있는가?"
   ├─ Yes → 분리
   └─ No → 유지

3. "다른 문서와 공통 패턴이 있는가?"
   ├─ Yes → 공통 Cell 추출
   └─ No → 유지

4. "여러 관점에서 참조되는가?"
   ├─ Yes → 독립 Cell로 승격
   └─ No → 상위 문서에 포함
```

---

## 7. 크기 기준 (부차적)

관심사 분리가 최우선이지만, 참고용 크기 기준:

- **너무 작음** (< 50줄): 상위 Cell에 섹션으로 포함 고려
- **적정** (50~500줄): 대부분의 단일 관심사
- **큰 편** (500~1000줄): 분리 가능한 하위 관심사 검토
- **너무 큼** (> 1000줄): 거의 확실히 여러 관심사 포함

**주의**: 크기는 절대 기준이 아님! 관심사가 하나면 1500줄도 OK.

---

## 8. 실전 예시: Business Logic 분해

### 초기 상태
```
business_logic.md (3000줄)
├─ 사용자 인증
├─ 결제 처리
├─ 알림 시스템
├─ 데이터 검증
└─ 권한 관리
```

### Step 1: 관심사별 분리
```
domain/
├─ business_logic.md (살아있는 개요, 300줄)
├─ user_authentication.md (600줄)
├─ payment_processing.md (800줄)
├─ notification_system.md (500줄)
├─ data_validation.md (700줄)
└─ permission_management.md (400줄)
```

### Step 2: 하위 관심사 분리
```
domain/
├─ business_logic.md (개요)
│
├─ auth/ (인증 관련)
│  ├─ user_authentication.md (개요)
│  ├─ jwt_token_issuance.md
│  ├─ session_lifecycle.md
│  └─ password_hashing.md
│
├─ payment/ (결제 관련)
│  ├─ payment_processing.md (개요)
│  ├─ payment_gateway_integration.md
│  ├─ refund_processing.md
│  └─ payment_validation.md
│
└─ validation/ (검증 관련)
   ├─ data_validation.md (개요)
   ├─ input_sanitization.md
   └─ business_rule_validation.md
```

### Step 3: 교차 관심사 추출
```
발견: payment_validation.md와 data_validation.md에 공통 로직!

추출:
common/
└─ validation_framework.md (신규!)
   └─ referenced_by: [payment_validation, data_validation, input_sanitization]
```

---

## 9. AI 에이전트 지침

### Turn 3 체크리스트

```markdown
## 지식 세포화 시 질문

1. **관심사 개수**
   - [ ] 이 내용이 다루는 핵심 관심사가 1개인가?
   - [ ] 여러 관심사가 섞여있다면 각각 분리했는가?

2. **독립성**
   - [ ] 각 Cell이 독립적으로 이해 가능한가?
   - [ ] 다른 Cell 없이도 해당 관심사를 설명할 수 있는가?

3. **재사용성**
   - [ ] 여러 관점에서 참조될 가능성이 있는가?
   - [ ] 다른 도메인에서도 활용 가능한가?

4. **공통 패턴**
   - [ ] 다른 Cell과 중복되는 로직이 있는가?
   - [ ] 있다면 공통 Cell로 추출했는가?

5. **연관성 네트워크**
   - [ ] 관련 Cell들을 `references`에 명시했는가?
   - [ ] 연관성 점수를 적절히 부여했는가?
   - [ ] 양방향 참조를 확인했는가?
```

---

## 10. 요약

| 원칙 | 설명 |
|------|------|
| **관심사 우선** | 크기가 아닌 관심사로 분리 |
| **살아있는 문서** | 개요 문서는 네트워크의 허브 역할 |
| **교차 추출** | 공통 패턴은 새로운 Cell로 |
| **다차원 연결** | 하나의 Cell이 여러 관점에서 참조됨 |
| **동적 진화** | 필요시 기존 Cell 분리/병합 |

**핵심**: "뇌처럼 생각하라. 각 Cell은 뉴런, 연관성은 시냅스, 전체는 살아있는 신경망."
